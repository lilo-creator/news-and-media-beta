<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}/events/</loc>
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
    </url>
    {% for event in events %}
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{{ event.get_absolute_url }}</loc>
        <lastmod>{{ event.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>
    {% endfor %}
    {% for tag in tags %}
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'events:events_by_tag' tag.slug %}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>
    {% endfor %}
</urlset>
